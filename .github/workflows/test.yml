name: 'Release Test'
on:
  create:
    tags:
      - 'v*'

jobs:
  downstream-releases:
    name: Create Downstream tags
    runs-on: ubuntu-latest
    steps:
      - name: Print Info
        run: |
          echo ${{ github.ref }}

      - name: Release Repo1
        uses: akhilerm/release-mgmt@main
        with:
          token: ${{ secrets.RELEASE_TOKEN }}
          tag_name: ${{ github.ref }}
          body: 'Released from release-mgmt'
